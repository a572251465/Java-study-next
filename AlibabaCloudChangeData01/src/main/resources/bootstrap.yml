server:
  port: 9000

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.01:8848
      config:
        server-addr: 127.0.0.01:8848
        file-extension: yaml

