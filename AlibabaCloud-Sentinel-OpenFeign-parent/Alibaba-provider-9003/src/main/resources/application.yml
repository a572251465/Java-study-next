spring:
  profiles:
    active: dev
  application:
    name: sentinel-openfeign-client

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoint:
    web:
      exposure:
        include: "*"

server:
  port: 9003
