server:
  port: 9004

spring:
  application:
    name: sentinel-openfeign-client

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoint:
    web:
      exposure:
        include: "*"