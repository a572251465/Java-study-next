server:
  port: 9000

spring:
  application:
    name: sentinel-cloud-client
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719


management:
  endpoint:
    web:
      exposure:
        include: "*"
